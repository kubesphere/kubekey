---
# load defaults vars
- import_playbook: hook/default.yaml

- hosts:
    - all
  vars_files:
    - vars/certs_renew.yaml

- hosts:
    - localhost
  tags: ["certs"]
  roles:
    - init/init-cert

- hosts:
    - all
  tags: ["certs"]
  roles:
    - role: certs/renew
