---
- import_playbook: hook/default.yaml

- hosts:
    - localhost
  roles:
    - init/init-artifact
    - init/init-cert
  tasks:
    - name: Export artifact
      command: |
        cd {{ .binary_dir }} && tar -czvf {{ .artifact_file }} *
