---
- import_playbook: hook/default.yaml

# load defaults vars
- hosts:
    - all
  vars_files:
    - vars/common.yaml
    - vars/kubernetes.yaml

- hosts:
   - image_registry
  roles:
    - role: uninstall/image-registry