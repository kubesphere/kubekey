cni:
  calico:
    values: |
      # calico helm values
      tigeraOperator:
        registry: {{ .quayio_registry }}
      calicoctl:
        image: {{ .dockerio_registry }}/calico/ctl
      installation:
        registry: {{ .dockerio_registry }}
        calicoNetwork:
          bgp: Enabled