image_registry:
  # Virtual IP address for repository High Availability. the Virtual IP address should be available.
  # http_port / https_port are computed in the global defaults role.
  harbor:
    data_dir: /var/lib/harbor/data
