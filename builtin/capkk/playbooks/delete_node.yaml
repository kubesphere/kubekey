---
- name: uninstall node
  hosts:
    - "{{ .provider_id }}"
  vars_files:
    - vars/main.yaml
  gather_facts: true
  roles:
    - uninstall/kubernetes
    - uninstall/cri
    - uninstall/cloud-config
