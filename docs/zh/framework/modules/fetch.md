# fetch 模块

从远程主机拉取文件到本地。

## 参数

| 参数 | 说明 | 类型 | 必填 | 默认值 |
|------|------|------|------|--------|
| src | 远程主机上的文件路径 | 字符串 | 是 | - |
| dest | 本地保存路径 | 字符串 | 是 | - |

## 示例

**1. 拉取文件**

```yaml
- name: fetch file
  fetch:
    src: /tmp/src.yaml
    dest: /tmp/dest.yaml
```
